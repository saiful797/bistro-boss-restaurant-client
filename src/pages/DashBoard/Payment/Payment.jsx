import SectionTitle from "../../../components/SectionTitle";

const Payment = () => {
    return (
        <div>
            <SectionTitle
                subHeading={'Payment'}
                heading={'Please pay to eat'}
            />
        </div>
    );
};

export default Payment;